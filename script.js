var store = {
  name: 'Pioneer Square';
  minCustomer: 17;
  maxCustomer: 88;
  avgCookie: 5.2;
  randomCust: [];
  cookiesNeeded: function (minCustomer, maxCustomer, avgCookie) {
    while (randomCust.length < 10) {
      var cookieSales = ((Math.floor(Math.random() * (maxCustomer - minCustomer) + minCustomer)) * avgCookie);
    randomCust.push(cookieSales);
    console.log(cookieSales);
    console.log(randomCust);
    }
  }
}

var store = {
  name: 'Portland Airport';
  minCustomer: 6;
  maxCustomer: 24;
  avgCookie: 1.2;
  randomCust: [];
  cookiesNeeded: function (minCustomer, maxCustomer, avgCookie) {
    while (randomCust.length < 10) {
      var cookieSales = ((Math.floor(Math.random() * (maxCustomer - minCustomer) + minCustomer)) * avgCookie);
    randomCust.push(cookieSales);
    console.log(cookieSales);
    console.log(randomCust);
    }
  }
}

var store = {
  name: 'Washington Square';
  minCustomer: 11;
  maxCustomer: 38;
  avgCookie: 1.9;
  randomCust: [];
  cookiesNeeded: function (minCustomer, maxCustomer, avgCookie) {
    while (randomCust.length < 10) {
      var cookieSales = ((Math.floor(Math.random() * (maxCustomer - minCustomer) + minCustomer)) * avgCookie);
    randomCust.push(cookieSales);
    console.log(cookieSales);
    console.log(randomCust);
    }
  }
}

var store = {
  name: 'Sellwood';
  minCustomer: 20;
  maxCustomer: 48;
  avgCookie: 3.3;
  randomCust: [];
  cookiesNeeded: function (minCustomer, maxCustomer, avgCookie) {
    while (randomCust.length < 10) {
      var cookieSales = ((Math.floor(Math.random() * (maxCustomer - minCustomer) + minCustomer)) * avgCookie);
    randomCust.push(cookieSales);
    console.log(cookieSales);
    console.log(randomCust);
    }
  }
}

var store = {
  name: 'Pearl District';
  minCustomer: 3;
  maxCustomer: 24;
  avgCookie: 2.6;
  randomCust: [];
  cookiesNeeded: function (minCustomer, maxCustomer, avgCookie) {
    while (randomCust.length < 10) {
      var cookieSales = ((Math.floor(Math.random() * (maxCustomer - minCustomer) + minCustomer)) * avgCookie);
    randomCust.push(cookieSales);
    console.log(cookieSales);
    console.log(randomCust);
    }
  }
}
